// Stub — NewMonitorModal is not implemented yet
export default function NewMonitorModal({
  open,
  onClose,
  onCreated,
}: {
  open: boolean;
  onClose: () => void;
  onCreated?: (monitorId: number) => void;
}) {
  return null;
}
